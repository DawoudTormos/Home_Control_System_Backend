version: "1"
packages:
  - name: "db"                     # Package name for the generated code
    path: "./db"                   # Directory where generated Go code will be placed
    queries: "./db/queries"        # Directory containing your SQL query files
    schema: "./db/schema"          # Directory containing your SQL schema files
    engine: "postgresql"           # Database engine

.